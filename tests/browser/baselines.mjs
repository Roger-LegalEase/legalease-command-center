// CCX-006 resolves the two exact contrast exceptions and removes the blocked
// external Geist request. New browser errors or serious/critical axe findings
// therefore fail without an allowlist.
export const consoleErrorBaseline = Object.freeze([]);

export const accessibilityBaseline = Object.freeze({
  today:Object.freeze([]),
  social:Object.freeze([])
});
