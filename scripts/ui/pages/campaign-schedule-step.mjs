import{escapeAttribute,escapeHtml}from"../html.mjs";export function renderCampaignScheduleStep(view={}){const fields=view.fields||{};return`<div class="campaign-schedule-step" data-campaign-schedule-step><fieldset><legend>When should this Campaign be ready?</legend>${(view.modes||[]).map((mode)=>`<label><input type="radio" name="mode" value="${escapeAttribute(mode.key)}"${mode.key===fields.mode?" checked":""}>${escapeHtml(mode.label)}</label>`).join("")}</fieldset><label>Date and time<input type="datetime-local" name="scheduledAt" value="${escapeAttribute(fields.scheduledAt||"")}"></label><label>Time zone<input name="timezone" value="${escapeAttribute(fields.timezone||"")}" placeholder="America/New_York"></label><fieldset><legend>Weekday sending window</legend><p>The existing Eastern Time send window remains authoritative.</p><label>Start hour ET<input type="number" name="startHourET" min="0" max="23" value="${escapeAttribute(fields.weekdayWindow?.startHourET??"")}"></label><label>End hour ET<input type="number" name="endHourET" min="1" max="24" value="${escapeAttribute(fields.weekdayWindow?.endHourET??"")}"></label></fieldset><details><summary>Advanced</summary><label><input type="checkbox" name="batchEnabled"${fields.batchPlan?.enabled?" checked":""}> Send in batches</label><label>Batch size<input type="number" name="batchSize" min="1" max="1000" value="${escapeAttribute(fields.batchPlan?.size??"")}"></label><p>Planning a batch does not enable sending.</p></details></div>`;}
