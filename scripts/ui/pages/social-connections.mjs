export const SOCIAL_CONNECTIONS_STYLESHEET_PATH = "assets/ui/social-connections.css";
const esc = (value = "") => String(value ?? "").replace(/[&<>"']/g, (character) => ({ "&":"&amp;", "<":"&lt;", ">":"&gt;", '"':"&quot;", "'":"&#39;" })[character]);
export function renderSocialConnectionsPage(contract = {}) {
  if (!contract.ok) return `<section class="social-connections"><h1>Social connections unavailable</h1><p>No private connection details were loaded.</p></section>`;
  return `<section class="social-connections" data-social-connections><header><p class="eyebrow">Settings</p><h1>Social connections</h1><p>Connection and publishing authority are separate. Only server-side configuration can enable publishing.</p></header><div class="social-connections-grid">${contract.connections.map((item) => `<article data-connection-state="${esc(item.state.key)}"><h2>${esc(item.label)}</h2><strong>${esc(item.state.label)}</strong><p>${item.state.key === "not_connected" ? "Connect through the reviewed provider flow." : item.state.key === "connected_publishing_off" ? "The account is connected; live publishing remains off." : item.state.key === "ready_to_publish" ? "Connection and server-side publishing gate are ready." : "Review the connection in the controlled provider flow."}</p>${item.connectionId ? `<a href="#settings/social/${encodeURIComponent(item.channel)}">Review connection</a>` : ""}</article>`).join("")}</div></section>`;
}
