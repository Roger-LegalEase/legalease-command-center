import{escapeAttribute,escapeHtml}from"../html.mjs";const value=(input)=>input===null||input===undefined||input===""?"Unavailable":escapeHtml(String(input));export function renderCampaignAdvancedDelivery(view={}){if(!view.advancedAuthorized)return`<section class="campaign-advanced-summary"><h2>Advanced delivery</h2><p>${escapeHtml(view.summary||"Advanced delivery details are unavailable.")}</p></section>`;return`<section class="campaign-advanced" data-campaign-advanced><h2>Advanced delivery</h2><p class="campaign-advanced-summary">${escapeHtml(view.summary)}</p><details><summary>Show technical delivery details</summary><dl><div><dt>Delivery tracking</dt><dd>${value(view.tracking?.status)}</dd></div><div><dt>Domain verification</dt><dd>${value(view.domain?.status)}</dd></div><div><dt>Bounce limit</dt><dd>${value(view.limits?.bounce)}</dd></div><div><dt>Complaint limit</dt><dd>${value(view.limits?.complaint)}</dd></div><div><dt>Suppressed</dt><dd>${value(view.suppression?.count)}</dd></div><div><dt>SendGrid event health</dt><dd>${value(view.eventHealth?.latestStatus)}</dd></div><div><dt>Batch count</dt><dd>${value(view.batch?.count)}</dd></div><div><dt>Connection</dt><dd>${value(view.connection?.status)}</dd></div><div><dt>Sending enabled</dt><dd>${value(view.connection?.sendingEnabled)}</dd></div></dl>${view.eventReferences?.length?`<h3>Authorized event references</h3><ol>${view.eventReferences.map((item)=>`<li data-event-reference="${escapeAttribute(item.id)}">${escapeHtml(item.id)} · ${value(item.status)}</li>`).join("")}</ol>`:""}<p>These details cannot override safety limits or enable sending.</p></details></section>`;}
