# Inspection bundle manifest — legalease-command-center

Collected 2026-07-23 (UTC) at commit `e620bde` on branch `hotfix/founder-targeted-reads`.
Collection was read-only outside `./inspection-bundle`; a before/after `git status` diff
confirms the only working-tree change is this bundle folder.

1. **01-repo-tree.txt — COLLECTED.** Full file tree (1,502 entries), excluding node_modules,
   .git, dist, build, and this bundle folder.

2. **02-config/ — COLLECTED.** package.json, package-lock.json, render.yaml. No Dockerfile,
   Procfile, or other deploy config exists in the repo (render.yaml is the only deploy config;
   verified its env sections contain names only — all secrets are `sync: false` with no values).

3. **03-env-var-names.txt — COLLECTED.** 187 unique env var NAMES from process.env references
   (dot, bracket, and destructuring forms), .env/.env.local/.env.example key names, and
   render.yaml envVars keys. Names only; no values were read past the `=` or copied.

4. **04-schema/ — COLLECTED.** All Supabase SQL (core table DDL, the one migration, recovery
   notes), lib/storage DDL + migration code, and schema-in-code (storage.mjs collection
   registry, company-memory.mjs models, schema check tooling). Zero row data. skipped.txt
   documents every skipped file (suppression CSVs, user xlsx lists, state JSON) and why.

5. **05-safety-gates.md — COLLECTED.** All three rules located with file/function/line and code
   pasted verbatim. (a) send approval: enforced in code, blocking, fail-closed, 3 layers.
   (b) deploy approval: enforced via render.yaml `autoDeploy: false` (platform config) plus a
   blocking read-only preflight script (prod-commit-gate.mjs) used by runbooks — not an
   automated pipeline hook. (c) social hard-fail gates (em-dash, AI-phrase, dignity, asset
   integrity): enforced in code as blocking conditions (throw / HTTP 400). One gap noted:
   manual "Publish Now" does not re-check the live-posting env flag.

6. **06-test-results.txt — COLLECTED (with one documented deviation).** Full `npm test` chain
   output captured; ALL suites passed, exit code 0. Deviation: run with the repo's own
   `SKIP_ENV_LOCAL_FILE=1` switch so spawned test servers do not load .env.local (production
   credentials) — required by the audit's "no network calls to production services" rule, and
   matching the repo's documented clean-worktree CI-gate practice. Side effect disclosed: the
   soc2 export test regenerates data/exports/soc2/legalease-soc2-readiness-snapshot-2026-07.md
   (a gitignored export), inherent to running the suite as defined.

7. **07-dependency-audit.txt — COLLECTED.** `npm audit`: 1 high-severity finding — sharp <0.35.0
   (libvips CVEs CVE-2026-33327/33328/35590/35591); fix is a breaking upgrade to sharp@0.35.3.

8. **08-reports-sample/ — PARTIAL.** No daily campaign report exists locally (generated by a
   scheduled cloud routine, delivered externally, not persisted in the repo) — documented in
   README.txt. ONE application log sample included (production cutover action log), redacted:
   emails/names/phones → [REDACTED] (none were present); Supabase project ref and Render
   service ID additionally masked out of caution.

9. **09-loose-ends.txt — COLLECTED.** Zero TODO/FIXME/HACK comments in source (verified by
   case-insensitive grep); zero orphan test references (all test imports and package.json test
   chain files resolve); stub/no-op inventory: 3 review-only intake upload types, RCAP
   connection placeholder card, toast-only cockpit priority buttons, unwired SendGrid test
   button, local-placeholder Wilma image generation, and minor items.

10. **10-manifest.md — COLLECTED.** This file.

Hard-rule compliance: read-only outside ./inspection-bundle (verified via git status diff);
no env var values read or copied; no contact/suppression data copied; no network calls to
production services (tests ran without .env.local; npm audit contacted only the npm registry);
no deploys; no git pushes.
